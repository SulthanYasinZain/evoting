import LoginForm from "@/components/loginForm";

export default async function Page() {
  return (
    <section className="flex justify-center items-center h-[89svh]">
      <LoginForm />
    </section>
  );
}
